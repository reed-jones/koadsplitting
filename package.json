{
  "name": "koad-split",
  "type": "module",
  "version": "0.0.1",
  "description": "progressive bundling with code splitting",
  "main": "index.js",
  "author": "Reed Jones reedjones@reedjones.com",
  "license": "MIT",
  "scripts": {
    "serve:dev": "yarn clean && node src/server.js",
    "svelte-server": "node svelte-server",
    "dev": "nodemon",
    "clean": "rimraf .bundled"
  },
  "dependencies": {
    "svelte-server": "./svelte-server"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
